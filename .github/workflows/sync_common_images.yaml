name: sync common images

on:
  workflow_dispatch:
    inputs:
      common_images:
        description: "Images to sync (comma-separated, e.g., node:24.12.0-alpine,python:3.14.2-slim)"
        required: true
        type: string

env:
  REGISTRY: ${{ vars.ALIYUN_CR_REGISTRY }}
  NAMESPACE: ${{ vars.ALIYUN_CR_NAMESPACE_MODU }}

jobs:
  sync-images:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Install regctl (for rate-limited image sync)
        run: |
          # 安装 regctl - 支持更好的速率控制
          curl -sL https://github.com/regclient/regclient/releases/latest/download/regctl-linux-amd64 -o /usr/local/bin/regctl
          chmod +x /usr/local/bin/regctl
          regctl version

      - name: Log in to Aliyun Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.ALIYUN_CR_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_PASSWORD }}

      - name: Configure regctl for Aliyun Registry
        run: |
          # 配置 regctl 登录阿里云镜像仓库
          regctl registry login ${{ env.REGISTRY }} \
            --user "${{ secrets.ALIYUN_CR_USERNAME }}" \
            --pass "${{ secrets.ALIYUN_CR_PASSWORD }}"
          
          # 配置请求限制（全局设置）
          regctl registry set --req-per-sec 2 ${{ env.REGISTRY }}
          
          echo "✅ regctl configured with rate limiting"

      - name: Sync images
        id: sync
        run: |
          set +e  # 不因单个命令失败而退出，我们需要收集所有结果

          IMAGE_LIST="${{ github.event.inputs.common_images }}"
          if [[ -z "$IMAGE_LIST" ]]; then
            echo "❌ common_images input is empty"
            exit 1
          fi

          echo "📋 Images to sync: $IMAGE_LIST"

          success_list=""
          failed_list=""
          success_count=0
          failed_count=0

          IFS=',' read -ra IMAGES <<< "$IMAGE_LIST"
          for image in "${IMAGES[@]}"; do
            # 清理空格，补充 tag
            image=$(echo "$image" | xargs)
            [[ -z "$image" ]] && continue
            [[ "$image" != *":"* ]] && image="${image}:latest"

            echo ""
            echo "📦 Processing: $image"

            # 生成目标镜像名：移除 registry 前缀，斜杠转连字符
            if [[ "$image" == *"."*"/"* ]]; then
              name_part=$(echo "$image" | cut -d'/' -f2-)
            else
              name_part="$image"
            fi
            target_name=$(echo "$name_part" | sed 's/\//-/g')
            target="${{ env.REGISTRY }}/${{ env.NAMESPACE }}/${target_name}"

            echo "  → Target: $target"
            echo "  ⏳ Syncing..."

            # 核心同步：使用 regctl 复制多平台镜像（支持速率限制）
            # 添加超时保护（10分钟）和进度监控
            start_time=$(date +%s)
            sync_output=$(mktemp)
            
            # 在后台运行进度监控（每30秒输出一次心跳）
            (
              while true; do
                sleep 30
                elapsed=$(($(date +%s) - start_time))
                echo "  ⏳ Still syncing... (${elapsed}s elapsed)" >&2
              done
            ) &
            progress_pid=$!
            
            # 执行同步命令（使用 regctl，支持速率限制和重试）
            # --rate-limit: 限制请求速率（每秒请求数:burst大小）
            # --retry-limit: 自动重试次数
            # --retry-delay: 重试延迟
            sync_success=false
            max_retries=5
            retry_delay=60  # 重试延迟（秒）
            
            for ((retry=1; retry<=max_retries; retry++)); do
              if timeout 900 regctl image copy \
                --verbosity info \
                --rate-limit 2:1 \
                "$image" "$target" > "$sync_output" 2>&1; then
                sync_success=true
                break
              else
                sync_exit_code=$?
                # 检查是否是 429 限流错误
                if grep -qiE "(429|too many|rate limit)" "$sync_output" 2>/dev/null; then
                  if [[ $retry -lt $max_retries ]]; then
                    current_delay=$((retry_delay * retry))  # 指数退避
                    echo "  ⚠️  Rate limited, waiting ${current_delay}s before retry $((retry+1))/${max_retries}..."
                    sleep $current_delay
                    continue
                  fi
                fi
                # 非限流错误或已用尽重试次数，退出循环
                break
              fi
            done
            
            # 停止进度监控（无论成功或失败）
            kill $progress_pid 2>/dev/null || true
            wait $progress_pid 2>/dev/null || true
            
            # 处理结果
            if [[ "$sync_success" == "true" ]]; then
              end_time=$(date +%s)
              duration=$((end_time - start_time))
              
              # 检查输出中是否有成功标识
              echo "  ✅ Synced successfully"
              # 显示摘要信息（只显示前几行关键信息，如果存在）
              if [[ -s "$sync_output" ]]; then
                if grep -qE "(digest:|sha256:)" "$sync_output" 2>/dev/null; then
                  grep -E "(digest:|sha256:)" "$sync_output" 2>/dev/null | head -3 | sed 's/^/    /' 2>/dev/null || true
                else
                  # 如果没有找到 digest，显示最后几行输出作为参考
                  tail -5 "$sync_output" 2>/dev/null | sed 's/^/    /' 2>/dev/null || true
                fi
              fi
              
              echo "  ⏱️  Completed in ${duration}s"
              success_list="${success_list}${image} -> ${target}\n"
              ((success_count++))
            else
              end_time=$(date +%s)
              duration=$((end_time - start_time))
              
              if [[ ${sync_exit_code:-0} -eq 124 ]]; then
                echo "  ⏱️  Timeout after ${duration}s"
                failed_list="${failed_list}${image} (timeout)\n"
              else
                echo "  ❌ Failed (exit code: ${sync_exit_code:-unknown}, took ${duration}s)"
                # 显示错误信息
                if [[ -s "$sync_output" ]]; then
                  echo "  Error output:"
                  tail -20 "$sync_output" | sed 's/^/    /' || true
                fi
                failed_list="${failed_list}${image}\n"
              fi
              ((failed_count++))
            fi
            
            # 清理临时文件
            rm -f "$sync_output"
            
            # 添加延迟避免触发速率限制 (429 Too Many Requests)
            echo "  💤 Waiting 10s before next image to avoid rate limiting..."
            sleep 10
          done

          # 输出到 GitHub 环境变量
          echo "success_count=$success_count" >> $GITHUB_OUTPUT
          echo "failed_count=$failed_count" >> $GITHUB_OUTPUT

          # 使用 heredoc 保存多行内容
          # 确保即使列表为空也能正常工作
          if [[ -n "$success_list" ]]; then
            {
              echo "success_list<<EOF"
              echo -e "$success_list"
              echo "EOF"
            } >> $GITHUB_OUTPUT
          else
            echo "success_list=" >> $GITHUB_OUTPUT
          fi

          if [[ -n "$failed_list" ]]; then
            {
              echo "failed_list<<EOF"
              echo -e "$failed_list"
              echo "EOF"
            } >> $GITHUB_OUTPUT
          else
            echo "failed_list=" >> $GITHUB_OUTPUT
          fi

          echo ""
          echo "📊 Summary: $success_count succeeded, $failed_count failed"
          
          # 调试信息
          echo "🔍 Debug: success_count=$success_count, failed_count=$failed_count"
          echo "🔍 Debug: Total images processed: $((success_count + failed_count))"

          # 如果全部失败则退出
          if [[ $success_count -eq 0 && $failed_count -gt 0 ]]; then
            echo "❌ All images failed to sync"
            exit 1
          fi
          
          # 如果有成功的，即使有失败的也返回成功（部分成功）
          if [[ $success_count -gt 0 ]]; then
            echo "✅ Sync completed with $success_count success(es)"
            exit 0
          fi
          
          # 如果没有任何镜像处理（理论上不应该发生）
          if [[ $success_count -eq 0 && $failed_count -eq 0 ]]; then
            echo "⚠️  No images were processed"
            exit 0
          fi
          
          exit 0

      - name: Summary
        if: always()
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          ## 🚀 Image Sync Results

          | Metric | Value |
          |--------|-------|
          | ✅ Succeeded | ${{ steps.sync.outputs.success_count || 0 }} |
          | ❌ Failed | ${{ steps.sync.outputs.failed_count || 0 }} |
          | Registry | `${{ env.REGISTRY }}` |
          | Namespace | `${{ env.NAMESPACE }}` |

          EOF

          if [[ -n "${{ steps.sync.outputs.success_list }}" ]]; then
            echo "### ✅ Succeeded" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "${{ steps.sync.outputs.success_list }}" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi

          if [[ -n "${{ steps.sync.outputs.failed_list }}" ]]; then
            echo "### ❌ Failed" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "${{ steps.sync.outputs.failed_list }}" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
