name: sync common images

on:
  workflow_dispatch:

env:
  REGISTRY: ${{ vars.ALIYUN_CR_REGISTRY }}
  NAMESPACE: ${{ vars.ALIYUN_CR_NAMESPACE_MODU }}
  COMMON_IMAGES: ${{ vars.ALIYUN_CR_COMMON_IMAGES }}

jobs:
  sync-images:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Aliyun Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.ALIYUN_CR_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_PASSWORD }}

      - name: Sync images
        id: sync
        run: |
          # 初始化 buildx
          docker buildx create --name multiarch --driver docker-container --use
          docker buildx inspect --bootstrap

          IMAGE_LIST="${{ env.COMMON_IMAGES }}"
          if [[ -z "$IMAGE_LIST" ]]; then
            echo "❌ COMMON_IMAGES is empty"
            exit 1
          fi

          echo "📋 Images to sync: $IMAGE_LIST"

          success_list=""
          failed_list=""
          success_count=0
          failed_count=0

          IFS=',' read -ra IMAGES <<< "$IMAGE_LIST"
          for image in "${IMAGES[@]}"; do
            # 清理空格，补充 tag
            image=$(echo "$image" | xargs)
            [[ -z "$image" ]] && continue
            [[ "$image" != *":"* ]] && image="${image}:latest"

            echo ""
            echo "📦 Processing: $image"

            # 生成目标镜像名：移除 registry 前缀，斜杠转连字符
            if [[ "$image" == *"."*"/"* ]]; then
              name_part=$(echo "$image" | cut -d'/' -f2-)
            else
              name_part="$image"
            fi
            target_name=$(echo "$name_part" | sed 's/\//-/g')
            target="${{ env.REGISTRY }}/${{ env.NAMESPACE }}/${target_name}"

            echo "  → Target: $target"

            # 核心同步：使用 imagetools 直接复制多平台 manifest
            if docker buildx imagetools create --tag "$target" "$image" 2>&1; then
              echo "  ✅ Synced"
              success_list="${success_list}${image} -> ${target}\n"
              ((success_count++))
            else
              echo "  ❌ Failed"
              failed_list="${failed_list}${image}\n"
              ((failed_count++))
            fi
          done

          # 输出到 GitHub 环境变量
          echo "success_count=$success_count" >> $GITHUB_OUTPUT
          echo "failed_count=$failed_count" >> $GITHUB_OUTPUT

          # 使用 heredoc 保存多行内容
          {
            echo "success_list<<EOF"
            echo -e "$success_list"
            echo "EOF"
          } >> $GITHUB_OUTPUT

          {
            echo "failed_list<<EOF"
            echo -e "$failed_list"
            echo "EOF"
          } >> $GITHUB_OUTPUT

          echo ""
          echo "📊 Summary: $success_count succeeded, $failed_count failed"

          # 如果全部失败则退出
          [[ $success_count -eq 0 && $failed_count -gt 0 ]] && exit 1
          exit 0

      - name: Summary
        if: always()
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          ## 🚀 Image Sync Results

          | Metric | Value |
          |--------|-------|
          | ✅ Succeeded | ${{ steps.sync.outputs.success_count || 0 }} |
          | ❌ Failed | ${{ steps.sync.outputs.failed_count || 0 }} |
          | Registry | `${{ env.REGISTRY }}` |
          | Namespace | `${{ env.NAMESPACE }}` |

          EOF

          if [[ -n "${{ steps.sync.outputs.success_list }}" ]]; then
            echo "### ✅ Succeeded" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "${{ steps.sync.outputs.success_list }}" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi

          if [[ -n "${{ steps.sync.outputs.failed_list }}" ]]; then
            echo "### ❌ Failed" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "${{ steps.sync.outputs.failed_list }}" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
